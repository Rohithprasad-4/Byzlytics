# SecureGate AI — Python dependencies
# Pinned versions per the project report's technology stack (Section 4).

# Backend API (Phase 8)
Flask==3.0.3
Flask-CORS==4.0.1
psycopg2-binary==2.9.9
python-dotenv==1.0.1

# Data engineering pipeline (Phase 5)
pandas==2.2.2
numpy==1.26.4

# Machine learning (Phase 6)
scikit-learn==1.5.1
joblib==1.4.2

# AI risk assessment engine (Phase 7)
openai==1.40.0

# Network capture (Phase 3)
# NOTE: Scapy requires Npcap (Windows: https://npcap.com) or libpcap
# (Linux/macOS) to be installed separately at the OS level.
scapy==2.5.0

# Daily PDF reporting (Phase 11)
reportlab==4.2.2

# Testing (Phase 12)
pytest==8.3.2
pytest-cov==5.0.0

"""SecureGate AI — Data Engineering Pipeline (Phase 5).

Five stages, run in strict order: collect -> validate -> clean ->
transform -> enrich. Output is a pandas DataFrame with exactly the 21
columns defined in ml.features.FEATURE_COLUMNS, ready for scoring.
"""
